{% extends 'base.html.twig' %}
{% block body %}
<div class="container-fluid bgList">
    <div class="row">
        <div class=" col-3">
            <p>{{ detailSortie.nom }}</p>
        </div>
        <div class="col-3">
            <p>{{ detailSortie.dateHeureDebut | date('d/m/Y') }}</p>
        </div>
        <div class="col-3">
            <a>{{ detailSortie.duree| date('d/m/Y') }}</a>
        </div>
        <div class="col-3">
        </div>
        <button class="btn"><a href="{{ path('inscriptionSortie',{'id': detailSortie.id})}}">Inscrire</a></button>
    </div>
</div>
{% endblock %}