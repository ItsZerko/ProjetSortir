{% extends 'base.html.twig' %}
{% block body %}
    <div class="container-fluid bgList">
        <div class="row">
            <div class=" col-3">
                <p>{{ sortie.nom }}</p>
            </div>
            <div class="col-3">
                <p>{{ sortie.dateHeureDebut | date('d/m/Y') }}</p>
            </div>
            <div class="col-3">
                <a>{{ sortie.duree| date('d/m/Y') }}</a>
            </div>
            <div class="col-3">
        </div>
    </div>
{% endblock %}